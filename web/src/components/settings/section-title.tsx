export function SettingsSectionTitle({ title }: { title: string }) {
  return (
    <h3 className="text-[11px] font-medium text-muted-foreground uppercase tracking-wider mb-1.5 px-0.5">
      {title}
    </h3>
  );
}
